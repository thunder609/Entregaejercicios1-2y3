import React, { useState } from "react";
import SignIn from "../conectado";
import Logout from "../noconectado";

const Login = () => {
  const [isLoggedIn, setIsLoggedIn] = useState(false);

  if (!isLoggedIn) {
    this.handleEvent={setIsLoggedIn} }
    else{
      this.handleEvent={setIsLoggedIn}
    }

 
};
 export default Login;